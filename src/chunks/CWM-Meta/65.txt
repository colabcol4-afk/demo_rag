been studied extensively (Kaplan et al., 2020; Hoffmann et al., 2022; Bi et al., 2024). These empirical laws 
enable the estimation of the expected loss for a given compute budget, the identification of the optimal scaling 
strategy between model and data size, and an informed selection of training hyper-parameters. Following Bi 
et al. (2024), we develop scaling laws for optimal hyper-parameter prediction for the pre-training of CWM. We adopt the compute budget formula C = M . D, where M is the model size represented as the number of 
non-embedding FLOP per token and D is the data scale corresponding to the total number of training tokens. For a decoder-only Transformer, the number of FLOP per token is approximated by 
                                     M =    6N ne +    6dLS    ,                                  (1) 
                                          linear term attention term 
where Nne is the number of parameters excluding embeddings, d is the model hidden dimension, S is the 
sequence length, and L is the number of layers. This formula explicitly accounts for the computational cost of 
self-attention, which constitutes a significant portion of the total compute, especially for smaller models and 
longer contexts where attention overhead is relatively more pronounced, as discussed in Bi et al.